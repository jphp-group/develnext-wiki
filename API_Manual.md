# »нструкци€ к API

>  ратка€ информаци€ о том, как работать с документацией по API дл€ DevelNext.

---

#### —труктура документации
ƒокументаци€ по API представл€ет из себ€ набор статей, где кажда€ стать€ описывает конкретный класс PHP. ¬ develnext все построено на работе с классами и объектами. «на€ класс объекта, вы можете прочитать полную документацию по нему - какие методы и свойства у него есть и что они означают.

---

####  лючевые слова
¬ этой документации встречаютс€ следующие ключевые слова, важно знать, что они означают:
- `double` - означает, что значение это вещественное число, не целое.
- `int` - это всегда целое число, отрицательное или положительное.
- `bool` - это булево значение, т.е. `true` или `false` (да или нет, правда или ложь). 
- `string` - это строка, т.е. любой текст, строки в php заключают в одинарные или двойные кавычки.
- `array` - это php массив.
- `“олько дл€ чтени€` - применимо дл€ свойств объектов, означает, что свойство нельз€ изменить через код, только вз€ть значение свойства.

---

#### ќписание класса
ѕри описании класса пишетс€ его короткое название, полное название и от какого класса он унаследован. Ќапример, `UXNode` - короткое название, `php\gui\UXNode` - полное название (дл€ `use php\gui\UXNode`). ¬ описании классов встречаютс€ названи€ других классов в виде ссылок, которые ведут на описание этих классов.

---

< [¬ернутьс€ назад](API).